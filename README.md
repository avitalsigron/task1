# Given an image of a choice create a histogram of the RGB colors displayed in the image
- Import the image we want to create a histogram from
Calculation of the cytogram according to RGB colors
view of the graph